# Home Portal 🦁

A beautiful, customizable browser start page with card-based link management.

## Features

- 🎨 Modern, gradient-based design
- 📱 Responsive layout that works on all devices
- 🔍 Built-in search functionality
- ➕ Easy link management (add/delete links)
- 💾 Local storage - your links are saved in your browser
- 🎯 Quick access to your favorite websites

## Setup

1. Open `index.html` in your browser
2. To set it as your browser's start page:
   - **Chrome/Edge**: Settings → On startup → Open a specific page → Add `index.html` path
   - **Firefox**: Preferences → Home → Homepage → Custom URLs → Enter `index.html` path
   - **Safari**: Preferences → General → Homepage → Enter `index.html` path

## Usage

- **Add a link**: Click the `+` button in the bottom right corner
- **Open a link**: Click on any card
- **Delete a link**: Hover over a card and click the `×` button
- **Search**: Type in the search bar to filter links, or press Enter to search Google

## Customization

All links are stored in your browser's local storage. You can also edit the default links in `script.js` if you want to change the initial set of links.

Enjoy your personalized start page! 🎉